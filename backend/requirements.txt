# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.0.0

# LLM — new google-genai SDK (Gemini 3)
google-genai>=1.0.0

# Agent orchestration
# langgraph: the StateGraph pipeline itself — core, required
# langchain-core: base types that langgraph depends on internally — required but never called directly
# langchain-google-genai: NOT used — we call google-genai SDK directly, not via LangChain wrappers
langgraph>=0.4.0
langchain-core>=0.3.0

# Web scraping
firecrawl-py>=1.0.0  # Optional — set FIRECRAWL_API_KEY to enable; falls back to Playwright
playwright>=1.48.0
html2text>=2020.1.16  # Markdown conversion for Playwright-only path

# HTML parsing and DOM manipulation
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Utilities
pillow>=10.0.0
numpy>=1.26.0
httpx>=0.27.0

# Cloud storage
google-cloud-storage>=2.18.0

# Observability (optional — set LANGCHAIN_TRACING_V2=true to enable)
langsmith>=0.1.0
